用HTML+CSS写出来图片上的样子(不能用浮动)
作业1：
	1、标签要语义化
	2、鼠标放到每一行上，字体颜色要变，同时后面的箭头要往后走（用background结合hover搞定）
	3、每一行都是链接
作业2：
	1、标签要语义化，特别上面两行文字需要考虑一下用什么标签，同时里面的单词都是可点的，鼠标放上去后要显示蓝色的背景
	2、这个效果里会用到标签类型转换，需要考虑如何能让图片在一行显示
	3、图片可以点，图片下边的黑色文字也可以点，按钮也可以点，红色的字体不可以点
作业3：
	1、标签要语义化
	2、标题的背景要切成图片
	3、这个效果里会也要用到标签类型转换，要考虑如何能在一行
	4、性格下边的标签块是链接，鼠标放上去后要变成白色加边框的样子
	5、图片右侧的标题是链接
	6、图片也可以点击
	7、最下面的列表的黑色文字是链接，需要有一个鼠标放上去后的状态（这块标签语义化需要考虑一下）
	